spring:
  application:
    name: discovery
eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
      defaultZone: http://discovery:8761/eureka/
  instance:
    hostname: localhost

server:
  port: 8761
